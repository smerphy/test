[project]
name = "ephorate-workspace"
version = "0.0.0"
description = "Ephorate monorepo workspace root (not published)"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["engine", "sdk-python", "control-plane", "mcp-proxy"]

[tool.uv]
package = false
