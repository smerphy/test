[project]
name = "praetor-workspace"
version = "0.0.0"
description = "Praetor monorepo workspace root (not published)"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["engine", "sdk-python", "control-plane"]

[tool.uv]
package = false
